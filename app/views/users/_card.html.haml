.mypage-card
  %p.title  支払い方法
  - if @default_card_information.present?
    .create-card
      .cardName
        %span 登録カード番号
        %br
        = "**** **** **** " + @default_card_information.last4
      .userName
        %span 有効期限
        %br
        - exp_month = @default_card_information.exp_month.to_s
        - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
        = exp_month + " / " + exp_year
      .effectiveDate
      = form_with url: delete_credit_cards_path, method: :delete, local: true do |f|
        = f.submit 'カードを削除する'
  - else
    .create-card
      登録されたカードはありません。
    .mypage-card__btn
      =link_to new_credit_card_path do
        クレジットカードを追加
    